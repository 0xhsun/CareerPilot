{
  "name": "careerpilot",
  "private": true,
  "version": "1.0.0",
  "description": "CareerPilot — one-command dev launcher for the FastAPI backend + Vite frontend",
  "scripts": {
    "start": "concurrently --names backend,frontend --prefix-colors cyan,magenta --kill-others-on-fail \"npm:start:backend\" \"npm:start:frontend\"",
    "start:backend": "cd backend && uv run uvicorn app.main:app --reload --port 8000",
    "start:frontend": "cd frontend && npm run dev",
    "setup": "cd backend && uv sync --group dev && cd ../frontend && npm install"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
